# Sliding Window (TypeScript)

Implemented file: `sliding-window.ts`

## Implemented items
- Function `maxSumSubarray(nums: number[], k: number)`

## How the code works
- `maxSumSubarray()`: uses fixed-size sliding window to compute maximum sum.
