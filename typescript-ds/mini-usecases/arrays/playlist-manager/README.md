# Playlist Manager (TypeScript)

Implemented file: `playlist-manager.ts`

## Implemented items
- Class `PlaylistManager`

## How the code works
- `add()`: adds an item into internal storage.
- `remove()`: removes and returns one item based on class behavior.
- `move()`: reorders a stored item from one index to another when indices are valid.
- `list()`: returns current stored items.
