# Mini use cases

Practical modules. Each folder has a `README.md` and an implementation file.

## `arrays/` — array / list focused

Examples that mostly use arrays or lists: sorting, filtering, scanning, reordering.

See **`arrays/README.md`** for the list of projects inside.

## Other mini use cases (root level)

- `login-system/` — active sessions (`Set`-style usage in code)
- `undo-redo/` — two stacks for history
- `social-network/` — follow graph
- `network-connectivity/` — union–find style connectivity
