# Mini use cases

Practical modules. Each folder has a `README.md` and `Program.cs`.

## `arrays/` — array / list focused

Examples that mostly use lists: sorting, filtering, scanning, reordering.

See **`arrays/README.md`** for the list of projects inside.

## Other mini use cases (root level)

- `login-system/` — active sessions (`HashSet`)
- `undo-redo/` — two stacks for history
- `social-network/` — follow relationships
- `network-connectivity/` — union–find connectivity
